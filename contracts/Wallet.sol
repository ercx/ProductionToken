pragma solidity ^0.4.15;


import "./ProductionToken.sol";

contract Wallet {
	ProductionToken token;

/*
    function merge(ProductionToken tokenA, ProductionToken tokenB, ProductionToken tokenC, uint256 value) {
        tokenA.burn(value);
        tokenB.burn(value);
        tokenC. /// ??????
    }
*/

    
    
}

